{
  "name": "@reggie-db/dbx-tools-appkit",
  "version": "0.1.0",
  "description": "AppKit server plugin: wraps the genie plugin's sendMessage as an agent tool with live tool-progress over SSE.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "keywords": [
    "databricks",
    "appkit",
    "appkit-plugin",
    "genie",
    "agent"
  ],
  "peerDependencies": {
    "@databricks/appkit": ">=0.30.0",
    "zod": "^4.3.0"
  },
  "dependencies": {
    "@reggie-db/dbx-tools-appkit-shared": "workspace:*",
    "mem0ai": "^3.0.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@databricks/appkit": "^0.33.0",
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "@types/pg": "^8.20.0",
    "express": "^5.0.0",
    "typescript": "^5.6.0",
    "zod": "^4.3.0"
  }
}
