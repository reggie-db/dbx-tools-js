/**
 * Shared AppKit UI primitives for `@dbx-tools/*` feature UI packages.
 * Re-exports `@databricks/appkit-ui/react` so consumers can depend on
 * `@dbx-tools/appkit-ui` for components, peers, and shared styling.
 */
export * from "@databricks/appkit-ui/react";
