/**
 * `@dbx-tools/sdk-shared`: zod schemas + inferred TypeScript
 * types regenerated from upstream `.d.ts` declarations.
 *
 * Codegen owns `../generated/` end to end (see `package.json`'s
 * `codegen` field for the input list and `devkit codegen` for the
 * driver). This barrel is the stable, hand-tracked entry point that
 * lets the package manifest expose generated schemas without
 * generated-file wiring.
 */

export * from "../generated/index.js";
