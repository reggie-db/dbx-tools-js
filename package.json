{
  "name": "dbx-tools-appkit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "catalog": {
    "@databricks/appkit": "^0.35",
    "@databricks/appkit-ui": "^0.35",
    "@types/express": "^5",
    "@types/bun": "latest",
    "@types/node": "latest",
    "express": "^5",
    "zod": "^4.3.6"
  },
  "scripts": {
    "pretypecheck": "bun run scripts/pretypecheck.ts",
    "typecheck": "bunx --bun tsc --noEmit",
    "clean": "bun scripts/clean.ts",
    "prebuild": "bun run typecheck",
    "build": "bun scripts/build.ts",
    "create": "bun scripts/create.ts",
    "version": "bunx --bun changeset version",
    "prerelease": "bun run build",
    "release": "bun scripts/release.ts",
    "tag": "bun scripts/tag.ts",
    "dev": "bun --elide-lines=0 run --filter '@dbx-tools/appkit-demo' dev"
  },
  "peerDependencies": {
    "@databricks/appkit": "catalog:"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "@databricks/sdk-experimental": "^0.17",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@types/semver": "^7.7.1",
    "commander": "^15.0.0-0",
    "jsonc-parser": "^3.3.1",
    "p-memoize": "^8.0.0",
    "prettier": "^3.8.3",
    "semver": "^7.8.1",
    "typescript": "^5"
  },
  "overrides": {
    "zod": "4.3.6"
  }
}
