{
  "name": "dbx-tools-appkit",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo for @reggie-db/dbx-tools-appkit, -ui, -shared, and a runnable demo.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.0.6",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @reggie-db/dbx-tools-appkit-demo dev",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
